---
description: Execute the task list to build the feature.
argument-hint: [scope]
---

Execute the Spec-Kit **implement** workflow for this repository.

Read the skill definition at `.agents/skills/speckit-implement/SKILL.md` and follow its
instructions exactly, start to finish. Treat everything below as the user input
(`$ARGUMENTS`) for that workflow; if it is empty, proceed using the active
feature's existing spec/plan context.

$ARGUMENTS
