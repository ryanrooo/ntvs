---
description: Generate the dependency-ordered task list from the plan.
argument-hint: [notes]
---

Execute the Spec-Kit **tasks** workflow for this repository.

Read the skill definition at `.agents/skills/speckit-tasks/SKILL.md` and follow its
instructions exactly, start to finish. Treat everything below as the user input
(`$ARGUMENTS`) for that workflow; if it is empty, proceed using the active
feature's existing spec/plan context.

$ARGUMENTS
