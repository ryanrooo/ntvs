---
description: Generate the technical implementation plan and design artifacts.
argument-hint: [constraints/notes]
---

Execute the Spec-Kit **plan** workflow for this repository.

Read the skill definition at `.agents/skills/speckit-plan/SKILL.md` and follow its
instructions exactly, start to finish. Treat everything below as the user input
(`$ARGUMENTS`) for that workflow; if it is empty, proceed using the active
feature's existing spec/plan context.

$ARGUMENTS
