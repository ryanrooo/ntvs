---
description: Convert the task list into tracked issues.
---

Execute the Spec-Kit **taskstoissues** workflow for this repository.

Read the skill definition at `.agents/skills/speckit-taskstoissues/SKILL.md` and follow its
instructions exactly, start to finish. Treat everything below as the user input
(`$ARGUMENTS`) for that workflow; if it is empty, proceed using the active
feature's existing spec/plan context.

$ARGUMENTS
