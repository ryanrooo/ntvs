---
description: Generate a quality checklist for the active spec/plan.
argument-hint: [focus]
---

Execute the Spec-Kit **checklist** workflow for this repository.

Read the skill definition at `.agents/skills/speckit-checklist/SKILL.md` and follow its
instructions exactly, start to finish. Treat everything below as the user input
(`$ARGUMENTS`) for that workflow; if it is empty, proceed using the active
feature's existing spec/plan context.

$ARGUMENTS
