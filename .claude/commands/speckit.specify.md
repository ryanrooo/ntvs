---
description: Create or update the feature spec from a natural-language description.
argument-hint: <feature description>
---

Execute the Spec-Kit **specify** workflow for this repository.

Read the skill definition at `.agents/skills/speckit-specify/SKILL.md` and follow its
instructions exactly, start to finish. Treat everything below as the user input
(`$ARGUMENTS`) for that workflow; if it is empty, proceed using the active
feature's existing spec/plan context.

$ARGUMENTS
