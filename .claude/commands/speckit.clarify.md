---
description: Resolve spec ambiguities via structured questioning; records answers in the spec.
argument-hint: [focus area]
---

Execute the Spec-Kit **clarify** workflow for this repository.

Read the skill definition at `.agents/skills/speckit-clarify/SKILL.md` and follow its
instructions exactly, start to finish. Treat everything below as the user input
(`$ARGUMENTS`) for that workflow; if it is empty, proceed using the active
feature's existing spec/plan context.

$ARGUMENTS
